#include <stdio.h>

/**
 * main - so betty, just say yes e no hard
 *
 * Return: Always (0)
 */

int main(void)
{
	int i;

	i = 48;

	while (i < 58)
	{
		putchar(i);
		i++;
	}

	putchar(10);

	return (0);
}
