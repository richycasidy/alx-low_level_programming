#include <stdio.h>

/**
 * main - so betty, just say yes e no hard
 *
 * Return: Always (0)
 */

int main(void)
{
	int i = 122;

	while (i > 96)
	{
		putchar(i);
		i--;
	}

	putchar(10);

	return (0);
}
